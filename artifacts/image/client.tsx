// Image artifacts client functionality is disabled for this constraint parser project

export default function ImageArtifact() {
  return (
    <div className="p-4 text-center text-gray-500">
      Image artifacts are disabled in this constraint parser project.
    </div>
  );
}
